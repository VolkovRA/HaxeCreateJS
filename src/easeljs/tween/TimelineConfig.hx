package easeljs.tween;

/**
 * The configuration properties to apply to Timeline instance (ex. {loop:-1, paused:true}).
 * These props are set on the corresponding instance properties except where specified.
 * 
 * Documentation: https://www.createjs.com/docs/tweenjs/classes/Timeline.html
 * @author VolkovRA
 */
typedef TimelineConfig =
{
	>AbstractTweenConfig,	
}